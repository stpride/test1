name: Test-A414

on: [workflow_dispatch]

defaults:
  run:
    shell: bash
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: my-test
        run: |
          echo "Hello World!"
