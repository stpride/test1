name: Test DD Out

on:
  workflow_call:
    inputs:
      resource:
        description: 'AWS resource group to use'
        required: true
        type: string

jobs:
  job5:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        shell: bash
        run: |
          echo "tfkey = $(fromJson(inputs.resource)['tfkey'])"
