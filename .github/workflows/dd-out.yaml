name: Test DD Out

on:
  workflow_call:
    inputs:
      resource:
        description: 'AWS resource group to use'
        required: true
        type: string
        
env:
  DATA: >
    ${{ fromJson('{
      "AAA": {
        "tfkey": "analytics",
        "tfdir": "fq/analytics"
      },
      "BBB": {
        "tfkey": "calculations",
        "tfdir": "fq/calculations"
      },
      "CCC": {
        "tfkey": "goofy",
        "tfdir": "fq/goofy"
      },
    }')[github.event.inputs.resource] }}
  
jobs:
  job5:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        shell: bash
        run: |
          echo $DATA
